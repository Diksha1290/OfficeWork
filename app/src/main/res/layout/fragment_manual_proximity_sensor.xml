<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/proxyLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/proximity"
        style="@style/Widget.TextViewHeadingNormalText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imgViewProximity"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="@string/txtManualProximitySensor_test"
        android:textSize="@dimen/about_us"
        />

    <com.officework.customViews.IconView
        android:id="@+id/imgViewProximity"

        android:layout_centerHorizontal="true"
        android:layout_marginTop="98dp"
        app:srcCompat="@drawable/ic_proximity"
        android:layout_width="135dp"
        android:layout_height="135dp"/>

</RelativeLayout>